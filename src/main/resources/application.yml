spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/lab_db
    username: postgres
    password: 123456

  data:
    redis:
      host: 127.0.0.1
      port: 6379

  ai:
    dashscope:
      api-key: sk-b78bdf9fa8f64a0fa6148fcb37e85177
      chat:
        options:
          model: qwen-max  # 可选：qwen-plus, qwen-max, qwen-turbo

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: mySecretKeyForJwtTokenGeneration2025
  expiration: 86400000